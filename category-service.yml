spring:
  application:
    name: category-service
  datasource:
    url: jdbc:h2:mem:testdb;Mode=PostgreSQL
    platform: h2
  jpa:
    hibernate:
      ddl-auto: create
      
server:
  port: 8013
  
eureka:
  client:
    service-url:
      default-zone: ${EUREKA_HOME}
