spring:
  application:
    name: location-service
  datasource:
    url: jdbc:h2:mem:testdb;Mode=PostgreSQL
    platform: h2
  jpa:
    hibernate:
      ddl-auto: create
      
server:
  port: 8040
  
eureka:
  client:
    service-url:
      default-zone: ${EUREKA_HOME}
