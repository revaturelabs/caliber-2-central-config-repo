spring:
  application:
    name: category-service
  datasource:
    url: jdbc:h2:mem:testdb;Mode=PostgreSQL
    platform: h2
  jpa:
    hibernate:
      ddl-auto: create

server:
  port: 10003
  
---
spring:
  profiles: prod

eureka:
  instance:
    prefer-ip-address: true