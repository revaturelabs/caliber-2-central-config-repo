spring:
  application:
    name: quality-audit-service

server:
  port: 10005 
  
---
spring:
  profiles: local

---
spring:
  profiles: dev
  datasource:
    username: ${DB_USER}
    password: ${DB_PASS}
    url: ${DB_URL}
    driver-class-name: oracle.jdbc.driver.OracleDriver
  
---
spring:
  profiles: prod

eureka:
  instance:
    prefer-ip-address: true